---
layout: default
---
{% include navbar.liquid %}

<div>
    <article>
        <h1>{{ page.title }}</h1>
        <p>Written on {{ page.date | date: "%B %d, %Y" }}</p>
        {{ content }}
    </article>
</div>


